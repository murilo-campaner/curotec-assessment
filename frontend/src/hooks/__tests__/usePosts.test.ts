import { describe, it, expect, vi } from 'vitest';

// Mock simples para testar a estrutura básica
describe('usePosts', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
