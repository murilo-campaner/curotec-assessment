[phases.setup]
nixPkgs = ["nodejs_20", "postgresql_15"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build", "npm run prisma:generate"]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
